-r requirements.txt
pytest>=8.0.0
# Required indirectly by fastapi.testclient.TestClient via starlette.testclient.
# Without httpx, importing TestClient fails at collection time.
httpx>=0.27.0
ruff>=0.5.0
